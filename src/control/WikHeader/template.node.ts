import { BaseHeader } from "@/comp/BaseHeader/builder";

const { ROOT_CLASS, PORT_CLASS, ROOT_HTML, CSS } = BaseHeader.template("MainHeader", { type: "wiki" });

export { ROOT_CLASS, PORT_CLASS, ROOT_HTML, CSS };
