import { BaseControl } from "webnetq-js";
import { } from 'uictmplt-loader!./template.mjs';

export default class UIDropBlockControl  extends BaseControl {
  _init() {
  }

  initConfig(config) {
  }
};
